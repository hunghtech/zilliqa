columns:
    user_id:
        label: Username
        select: username
        relation: user
    fullname:
        label: Fullname
        select: name
        relation: user
    daily:
        label: daily
        type: number
    created_at:
        label: created_at
        type: datetime
